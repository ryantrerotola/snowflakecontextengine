Generate a Snowflake Cortex Analyst Semantic View YAML model from the following context.

The YAML should follow the Snowflake semantic model specification with:
- name and description at the top level
- tables with base_table references (database, schema, table)
- dimensions with name, description, expr, data_type, and optional synonyms/sample_values
- time_dimensions for date/timestamp columns
- measures with aggregation expressions
- relationships defining joins between tables

Context facts:
{facts}

Schema information:
{schema}

Generate a complete, valid YAML semantic model. Use the context to:
1. Write rich descriptions for tables, dimensions, and measures
2. Add synonyms based on business terminology
3. Define measures with correct aggregation expressions based on metric definitions
4. Include business rules as descriptions or filter conditions
5. Set is_enum=true for dimensions with known finite values

Return ONLY valid YAML, no explanatory text.